import { MapProviderProps } from "../types";

export const CesiumProvider = ({ className }: MapProviderProps) => {
  return (
    <section className={className}>
      <h3>3D Globe Provider (Cesium)</h3>
      <p>Placeholder — Cesium implementation pending.</p>
    </section>
  );
};
